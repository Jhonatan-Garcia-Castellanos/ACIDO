# ============================================================
# .env — CONFIGURACIÓN LOCAL (NO se sube a git, ver .gitignore)
# Generado desde .env.example
# ============================================================

# === BASE DE DATOS ===
DB_HOST=localhost
DB_NAME=proyecto_acido
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# === SMTP (Mailtrap: Email Testing → Inboxes → SMTP Settings → PHPMailer) ===
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=d5972b90cd42c3
MAIL_PASSWORD=7bc928a7a79cd3
MAIL_ENCRYPTION=tls
MAIL_FROM=no-reply@acido.local
MAIL_FROM_NAME="ACIDO Colombia"

# === APP ===
APP_URL=http://localhost/ACIDO/BACKPHP_TEST
